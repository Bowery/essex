dependencies:
  override:
    - pip install fabric
    - pip install requests
test:
  override:
    - echo "No Tests... for now"
deployment:
  production:
    branch: master
    commands:
      - fab deploy
